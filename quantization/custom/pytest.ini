[pytest]
log_cli = true
log_level = INFO
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
; log_file_mode = w
log_file_level = DEBUG
markers = 
    nvfp4: mark test as nvfp4 (deselect with '-m "not nvfp4"')
addopts = 
    --setup-show
    --capture=no
